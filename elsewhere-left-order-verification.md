# Elsewhere 左对齐与内容顺序验证

桌面截图确认：Elsewhere 双列内容组已从页面右下区域移回公共内容左侧，网格左边界约为 392px，靠近侧边导航；右侧内容区域保持空闲，可继续放置 Elsewhere 的管理总图。两列仍保持约 3cm（113.386px）水平间距和 RTL 行向。

每条 entry 的视觉顺序已确认：图片位于最上方，日期位于图片下方，若存在正文首句则显示在日期下方。当前两条记录均符合这一顺序。

手机截图确认：375px 视口下自动回退为单列，图片、日期和正文均完整显示，页面没有横向溢出。

自动化结果：相关 3 个测试文件共 9 项测试通过；生产构建通过；浏览器验证显示 `gridLeft=392`、`rowGap=113.386px`、桌面方向为 `column-reverse` + `rtl`，移动端方向为 `column` + `ltr`。

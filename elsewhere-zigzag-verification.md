# Elsewhere Z 字形布局视觉验证

桌面截图确认：Elsewhere 缩略图实际渲染高度约为 265px，对应 7cm；当前最早日期位于右侧，较新的日期位于左侧，符合从右下角向左排列的底部起始规则。网格方向为 `rtl`，外层行方向为 `column-reverse`，后续行会在上方继续排列，从而形成右下到左下、再右上到左上的 Z 字形流向。

手机截图确认：布局在 375px 宽度下回退为单列，日期和图片均完整显示，图片保持原始比例且没有横向溢出。移动端按时间顺序从较早条目到较新条目纵向展示，避免强行保留桌面双列造成拥挤。

自动化结果：桌面网格方向为 `column-reverse`，行方向为 `rtl`，首行日期从右到左为 `August 5, 2026`、`August 12, 2026`，缩略图尺寸为 199×265px 与 181×265px；手机端 `scrollWidth` 等于视口宽度 375px，外层方向为 `column`，单行列宽为 335px。
